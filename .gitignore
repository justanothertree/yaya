# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies & build output
node_modules
dist
dist-ssr
*.local

# Environment
.env*

# Generated member credentials — never commit
members-credentials.csv

# Editor / IDE
.vscode/*
!.vscode/extensions.json
.vs/
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.code-workspace

# Scratch files
commit-msg.txt

# The group's real Circuit history (names + every logged workout). Kept locally as the
# generator source for publicSeed.ts, never committed: only Evan's own slice is public.
src/circuit/seed.ts

# Visual Studio local IDE state — machine-specific, never useful to anyone else
.vs/

# import dry-run output, written next to whatever CSV you point it at
*.parsed.json
